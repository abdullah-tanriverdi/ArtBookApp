package com.tanriverdi.artbook

// Data class representing an artwork
class Art(val name: String, val id: Int) {
    // The primary constructor initializes the properties of the class
    // 'name': The name of the artwork
    // 'id': The unique identifier for the artwork

    // No additional methods or fields are currently defined as this class primarily serves as a data container
}
